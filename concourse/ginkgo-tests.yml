---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: "1.6.3"

inputs:
  - name: aker-oauth-plugins

run:
    path: aker-oauth-plugins/concourse/ginkgo-tests.sh
